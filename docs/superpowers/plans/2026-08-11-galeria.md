# Galeria (`GalleryPhoto`) — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar à `/galeria` um model feito para foto — imagem, legenda, ordem, publicado — no lugar do `Project`, que é um estudo de caso de agência com cliente, ano, resumo e página de detalhe.

**Architecture:** Mesma fatia vertical do PR do cardápio, que acabou de ser entregue e serve de precedente direto no repo: model Prisma com JSON localizado → validação `zod` + ponte de formulário (TDD) → DAL em `src/lib/queries.ts` com `unstable_cache` + tag → server actions com `getCurrentUser()` e `updateTag` → CRUD no admin → site público. `Project` **não** é removido aqui; ele apenas deixa de ser lido, e sai no PR seguinte.

**Tech Stack:** Next 16 (App Router + Turbopack), React 19 + React Compiler, Prisma 6 + PostgreSQL, next-intl (só `pt`), react-hook-form + zod, Vitest, Tailwind 4.

## Global Constraints

- **Branch:** `Development`. **Nunca `git push`** — o dono do projeto envia manualmente.
- **Validação obrigatória** ao fim de cada tarefa: `npm run typecheck && npm run lint && npm run build && npm test`.
- **Banco local de pé**: `docker compose up -d` (container `n8x-marketing-db`, porta 5433).
- 🛑 **Nunca** rodar `prisma migrate reset`, nunca definir `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION`, nunca aceitar prompt de reset. Se o Prisma pedir reset, **pare e reporte BLOCKED**.
- **Nunca editar migração já aplicada** (quebra de checksum).
- **Português apenas.** Toda string de UI em `src/messages/pt.json`; não existe `en.json`. String visível hardcoded é defeito.
- **Nunca devolver linha crua do Prisma ao cliente** — sempre view-model.
- **Toda action de admin** começa com `getCurrentUser()`; sem usuário, retorna `unauthorized`. Toda escrita revalida com `zod` no servidor e chama `updateTag(tags.gallery)`.
- **Nenhum formulário do admin usa `zodResolver`** — o servidor é a única validação real. Não introduza um; é decisão de produto que afeta cinco formulários.
- Commits convencionais terminando com `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Prefixo do board: **CRE**.

---

## Mapa de arquivos

**Criados:**

| Arquivo | Responsabilidade |
|---|---|
| `src/lib/validations/gallery.ts` | schema zod da foto |
| `src/lib/gallery-form.ts` | ponte formulário ↔ input |
| `src/app/actions/gallery.ts` | server actions de CRUD |
| `src/components/admin/gallery-photo-form.tsx` | formulário da foto |
| `src/components/admin/gallery-photo-delete-button.tsx` | exclusão |
| `src/components/gallery-photo-card.tsx` | card público |
| `src/app/[locale]/admin/(dashboard)/galeria/page.tsx` | listagem |
| `src/app/[locale]/admin/(dashboard)/galeria/new/page.tsx` | nova foto |
| `src/app/[locale]/admin/(dashboard)/galeria/[id]/page.tsx` | editar foto |
| `test/gallery-form.test.ts` | testes da ponte |

**Editados:** `prisma/schema.prisma`, `src/lib/cache.ts`, `src/lib/queries.ts`, `src/lib/admin-queries.ts`, `src/components/admin/admin-nav.tsx`, `src/app/[locale]/(marketing)/galeria/page.tsx`, `src/components/sections/portfolio-preview.tsx`, `src/app/sitemap.ts`, `src/app/llms.txt/route.ts`, `src/app/llms-full.txt/route.ts`, `src/messages/pt.json`.

**Removido:** `src/app/[locale]/(marketing)/galeria/[slug]/page.tsx`.

⚠️ **`llms.txt` e `llms-full.txt` estão nessa lista de propósito.** No PR do cardápio, três superfícies publicavam URLs de detalhe (`sitemap`, `llms.txt`, `llms-full.txt`) e o plano nomeou só o sitemap — as outras duas ficaram anunciando rotas que davam 404 e só a revisão final pegou. Aqui é o mesmo padrão: **as três** consomem `Project`.

---

### Task 1: Model e migração

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `prisma/migrations/<timestamp>_add_gallery/migration.sql` (gerada pelo Prisma)

**Interfaces:**
- Consumes: nada.
- Produces: o model `GalleryPhoto`; o client passa a expor `prisma.galleryPhoto`.

- [ ] **Step 1: Confirmar o banco de pé**

```bash
docker compose up -d
docker ps --filter "name=n8x-marketing-db" --format "{{.Status}}"
```

Esperado: `Up ... (healthy)`.

- [ ] **Step 2: Adicionar o model**

Em `prisma/schema.prisma`, na seção de conteúdo, junto de `MenuCategory`/`MenuItem`:

```prisma
/// Uma foto da galeria (salão, fachada, ambiente, pratos). Substitui `Project`,
/// que era um estudo de caso de agência: uma foto não tem cliente, ano, resumo
/// nem página de detalhe.
model GalleryPhoto {
  id        String   @id @default(cuid())
  image     String
  caption   Json     @default("{}") // LocalizedText, opcional
  order     Int      @default(0)
  published Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([published, order])
  @@map("gallery_photos")
}
```

Note que **não há `slug`**: a foto não tem página própria, então não há URL para identificar. Isso também significa que nenhuma action precisa tratar violação de unicidade.

- [ ] **Step 3: Gerar e aplicar a migração**

```bash
npx prisma migrate dev --name add_gallery
```

🛑 Se pedir reset, **pare e reporte BLOCKED**.

- [ ] **Step 4: Conferir**

```bash
docker exec n8x-marketing-db psql -U agency -d agency -c "\dt"
```

Esperado: 14 tabelas — as 13 anteriores mais `gallery_photos`.

- [ ] **Step 5: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "CRE: model GalleryPhoto

Substitui Project na galeria. Project carregava clientName, year, summary,
content, tags e um [slug] com pagina propria — estrutura de estudo de caso
de agencia. Uma foto do salao nao tem cliente nem ano; eram seis campos
mortos para usar dois.

Sem slug: a foto nao tem pagina propria, entao nao ha URL para identificar
nem violacao de unicidade para tratar.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Validação e ponte do formulário (TDD)

Escreva os testes **antes** da implementação.

**Files:**
- Create: `src/lib/validations/gallery.ts`
- Create: `src/lib/gallery-form.ts`
- Test: `test/gallery-form.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `galleryPhotoSchema`, `type GalleryPhotoInput`, `GalleryPhotoFormValues`, `emptyGalleryPhotoForm()`, `photoToForm(row)`, `photoFormToInput(values)`.

- [ ] **Step 1: Escrever os testes que falham**

Criar `test/gallery-form.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  emptyGalleryPhotoForm,
  photoToForm,
  photoFormToInput,
} from "@/lib/gallery-form";
import { galleryPhotoSchema } from "@/lib/validations/gallery";

describe("foto da galeria", () => {
  it("o formulário vazio não passa: a imagem é obrigatória", () => {
    const input = photoFormToInput(emptyGalleryPhotoForm());
    expect(galleryPhotoSchema.safeParse(input).success).toBe(false);
  });

  it("com imagem, passa mesmo sem legenda — a legenda é opcional", () => {
    const values = emptyGalleryPhotoForm();
    values.image = "https://exemplo.com/salao.webp";
    const input = photoFormToInput(values);
    expect(input.caption.pt).toBe("");
    expect(galleryPhotoSchema.safeParse(input).success).toBe(true);
  });

  it("recusa uma imagem que não é URL", () => {
    const values = emptyGalleryPhotoForm();
    values.image = "salao.webp";
    expect(galleryPhotoSchema.safeParse(photoFormToInput(values)).success).toBe(
      false,
    );
  });

  it("converte a ordem para número e apara a legenda", () => {
    const values = emptyGalleryPhotoForm();
    values.image = "https://exemplo.com/salao.webp";
    values.caption.pt = "  O salão renovado  ";
    values.order = "7";
    const input = photoFormToInput(values);
    expect(input.order).toBe(7);
    expect(input.caption.pt).toBe("O salão renovado");
  });

  it("photoToForm lê uma legenda ausente como texto vazio", () => {
    // `caption` tem default `{}` no Prisma: a chave `pt` só existe depois que
    // alguém salva o formulário. Ler isso como undefined quebraria o input.
    const form = photoToForm({
      image: "https://exemplo.com/fachada.webp",
      caption: {},
      order: 2,
      published: false,
    });
    expect(form.caption.pt).toBe("");
    expect(form.order).toBe("2");
    expect(form.published).toBe(false);
  });

  it("photoToForm devolve a legenda existente", () => {
    const form = photoToForm({
      image: "https://exemplo.com/mesa.webp",
      caption: { pt: "A mesa posta" },
      order: 0,
      published: true,
    });
    expect(form.caption.pt).toBe("A mesa posta");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npx vitest run test/gallery-form.test.ts
```

Esperado: FALHA com "Failed to resolve import @/lib/gallery-form".

- [ ] **Step 3: Criar o schema**

Criar `src/lib/validations/gallery.ts`:

```ts
import { z } from "zod";
import { locales, type Locale } from "@/i18n/routing";

/**
 * Validação do editor da galeria. Uma foto tem imagem (obrigatória), legenda
 * opcional e os flags de ordem/publicação.
 *
 * O formulário do cliente coleta valores planos e os mapeia para esta forma
 * antes de enviar; a server action revalida com o mesmo schema, como boundary.
 */

/** Opcional em todos os locales — a legenda pode ficar em branco. */
function optionalLocalizedText(max: number) {
  return z.object(
    Object.fromEntries(
      locales.map((l) => [l, z.string().trim().max(max)]),
    ) as Record<Locale, z.ZodString>,
  );
}

export const galleryPhotoSchema = z.object({
  image: z.string().trim().url().max(500),
  caption: optionalLocalizedText(200),
  order: z.coerce.number().int().min(0).max(9999),
  published: z.boolean(),
});

export type GalleryPhotoInput = z.infer<typeof galleryPhotoSchema>;
```

- [ ] **Step 4: Criar a ponte**

Criar `src/lib/gallery-form.ts`:

```ts
import { locales, type Locale } from "@/i18n/routing";
import type { GalleryPhotoInput } from "@/lib/validations/gallery";

/**
 * Ponte entre o formulário da galeria e a forma armazenada (número como texto,
 * mapa localizado). Sem "use client" nem "server-only" — os dois lados importam.
 */

type LocalizedStrings = Record<Locale, string>;

function blankLocalized(): LocalizedStrings {
  return Object.fromEntries(locales.map((l) => [l, ""])) as LocalizedStrings;
}

function readLocalized(value: unknown): LocalizedStrings {
  const obj = (value ?? {}) as Record<string, unknown>;
  return Object.fromEntries(
    locales.map((l) => [l, typeof obj[l] === "string" ? (obj[l] as string) : ""]),
  ) as LocalizedStrings;
}

function trimLocalized(value: LocalizedStrings): LocalizedStrings {
  return Object.fromEntries(
    locales.map((l) => [l, value[l].trim()]),
  ) as LocalizedStrings;
}

export type GalleryPhotoFormValues = {
  image: string;
  caption: LocalizedStrings;
  order: string;
  published: boolean;
};

export function emptyGalleryPhotoForm(): GalleryPhotoFormValues {
  return {
    image: "",
    caption: blankLocalized(),
    order: "0",
    published: true,
  };
}

type GalleryPhotoRow = {
  image: string;
  caption: unknown;
  order: number;
  published: boolean;
};

export function photoToForm(p: GalleryPhotoRow): GalleryPhotoFormValues {
  return {
    image: p.image,
    caption: readLocalized(p.caption),
    order: String(p.order),
    published: p.published,
  };
}

export function photoFormToInput(
  values: GalleryPhotoFormValues,
): GalleryPhotoInput {
  return {
    image: values.image.trim(),
    caption: trimLocalized(values.caption),
    order: Number(values.order),
    published: values.published,
  };
}
```

- [ ] **Step 5: Rodar os testes**

```bash
npx vitest run test/gallery-form.test.ts
```

Esperado: PASS, 6 testes.

- [ ] **Step 6: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: `Test Files 5 passed (5)`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/validations/gallery.ts src/lib/gallery-form.ts test/gallery-form.test.ts
git commit -m "CRE: validacao e ponte de formulario da galeria

Escrito por TDD. Os testes cobrem o que erra calado: legenda ausente lida
como texto vazio (caption tem default {} no Prisma, entao a chave pt so
existe depois do primeiro save), ordem convertida para numero, e imagem que
nao e URL recusada.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Camada de acesso a dados

**Files:**
- Modify: `src/lib/cache.ts`, `src/lib/queries.ts`, `src/lib/admin-queries.ts`

**Interfaces:**
- Consumes: o model da Task 1.
- Produces: `tags.gallery` (`"gallery"`); `getGalleryPhotos(locale): Promise<GalleryPhotoView[]>`; o tipo `GalleryPhotoView`; `getAdminGalleryPhotos()`, `getGalleryPhotoById(id)`.

- [ ] **Step 1: Adicionar a tag**

Em `src/lib/cache.ts`, dentro de `tags`:

```ts
  /** Fotos da galeria. */
  gallery: "gallery",
```

- [ ] **Step 2: Adicionar o view-model e a query pública**

Em `src/lib/queries.ts`, junto dos outros tipos exportados:

```ts
export type GalleryPhotoView = {
  id: string;
  image: string;
  caption: string;
};
```

E junto das demais funções cacheadas:

```ts
/** As fotos publicadas da galeria, na ordem definida no admin. */
export const getGalleryPhotos = unstable_cache(
  async (locale: Locale): Promise<GalleryPhotoView[]> => {
    const rows = await prisma.galleryPhoto.findMany({
      where: { published: true },
      orderBy: { order: "asc" },
    });
    return rows.map((p) => ({
      id: p.id,
      image: p.image,
      caption: localize(p.caption, locale),
    }));
  },
  ["gallery"],
  { tags: [tags.gallery], revalidate },
);
```

- [ ] **Step 3: Adicionar as queries do admin**

Em `src/lib/admin-queries.ts` (adicione `GalleryPhoto` ao import de tipos do `@prisma/client`):

```ts
export async function getAdminGalleryPhotos(): Promise<GalleryPhoto[]> {
  return prisma.galleryPhoto.findMany({ orderBy: { order: "asc" } });
}

export async function getGalleryPhotoById(
  id: string,
): Promise<GalleryPhoto | null> {
  return prisma.galleryPhoto.findUnique({ where: { id } });
}
```

- [ ] **Step 4: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/cache.ts src/lib/queries.ts src/lib/admin-queries.ts
git commit -m "CRE: DAL da galeria

getGalleryPhotos devolve view-model, nunca linha crua do Prisma, e carrega a
tag 'gallery' para as escritas do admin invalidarem a pagina.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Server actions

**Files:**
- Create: `src/app/actions/gallery.ts`

**Interfaces:**
- Consumes: `galleryPhotoSchema`, `GalleryPhotoInput` (Task 2); `tags.gallery` (Task 3).
- Produces: `createGalleryPhoto`, `updateGalleryPhoto`, `deleteGalleryPhoto`, `GalleryActionResult`.

**Precedente no repo:** `src/app/actions/menu.ts` faz exatamente isto para o cardápio — leia antes. A diferença é que a galeria **não tem slug**, portanto não há caso `duplicate`.

- [ ] **Step 1: Criar as actions**

Criar `src/app/actions/gallery.ts`:

```ts
"use server";

import { updateTag } from "next/cache";
import { prisma } from "@/lib/prisma";
import { getCurrentUser } from "@/lib/auth";
import { tags } from "@/lib/cache";
import {
  galleryPhotoSchema,
  type GalleryPhotoInput,
} from "@/lib/validations/gallery";

export type GalleryActionResult =
  | { ok: true; id: string }
  | { ok: false; error: "unauthorized" | "invalid" | "unknown" };

/** `updateTag` (e não `revalidateTag`) para o admin ver a própria escrita na
 * visita seguinte, não na outra. */
function revalidateGallery(): void {
  updateTag(tags.gallery);
}

function photoData(input: GalleryPhotoInput) {
  return {
    image: input.image,
    caption: input.caption,
    order: input.order,
    published: input.published,
  };
}

export async function createGalleryPhoto(
  input: GalleryPhotoInput,
): Promise<GalleryActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = galleryPhotoSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const p = await prisma.galleryPhoto.create({ data: photoData(parsed.data) });
    revalidateGallery();
    return { ok: true, id: p.id };
  } catch (error) {
    console.error("Failed to create gallery photo", error);
    return { ok: false, error: "unknown" };
  }
}

export async function updateGalleryPhoto(
  id: string,
  input: GalleryPhotoInput,
): Promise<GalleryActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = galleryPhotoSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const p = await prisma.galleryPhoto.update({
      where: { id },
      data: photoData(parsed.data),
    });
    revalidateGallery();
    return { ok: true, id: p.id };
  } catch (error) {
    console.error("Failed to update gallery photo", error);
    return { ok: false, error: "unknown" };
  }
}

export async function deleteGalleryPhoto(id: string): Promise<{ ok: boolean }> {
  const user = await getCurrentUser();
  if (!user) return { ok: false };

  try {
    await prisma.galleryPhoto.delete({ where: { id } });
    revalidateGallery();
    return { ok: true };
  } catch (error) {
    console.error("Failed to delete gallery photo", error);
    return { ok: false };
  }
}
```

- [ ] **Step 2: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

- [ ] **Step 3: Commit**

```bash
git add src/app/actions/gallery.ts
git commit -m "CRE: server actions da galeria

Toda action passa por getCurrentUser() e revalida o zod no servidor, mesmo o
cliente ja tendo validado — o schema e o boundary, nao a conveniencia do
formulario.

Sem caso 'duplicate': a foto nao tem slug, entao nao ha unicidade para
violar.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Admin — CRUD da galeria

**Files:**
- Read first (padrão): `src/components/admin/menu-category-form.tsx`, `src/components/admin/menu-category-delete-button.tsx`, `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx` e suas páginas `new`/`[id]`, `src/components/admin/image-upload-field.tsx`
- Create: `src/components/admin/gallery-photo-form.tsx`, `src/components/admin/gallery-photo-delete-button.tsx`, `src/app/[locale]/admin/(dashboard)/galeria/page.tsx`, `.../galeria/new/page.tsx`, `.../galeria/[id]/page.tsx`
- Modify: `src/components/admin/admin-nav.tsx`, `src/messages/pt.json`

**Interfaces:**
- Consumes: `GalleryPhotoFormValues`, `emptyGalleryPhotoForm`, `photoToForm`, `photoFormToInput` (Task 2); `getAdminGalleryPhotos`, `getGalleryPhotoById` (Task 3); as três actions (Task 4).
- Produces: a rota `/admin/galeria`.

**Padrão:** o CRUD do cardápio, entregue no PR anterior, é o modelo mais próximo — mesma estrutura de `mode: "create" | "edit"`, `useTransition`, `serverError`, `router.push` no sucesso. Reproduza-a; não invente outra.

⚠️ `/admin/projects` **continua existindo** nesta tarefa — ele só sai no PR seguinte. Os dois aparecem no menu por um PR. É deliberado, pelo mesmo motivo que `/admin/services` conviveu com `/admin/cardapio`.

- [ ] **Step 1: Adicionar as traduções**

Em `src/messages/pt.json`, dentro de `admin`, um namespace novo `galeria`:

```json
    "galeria": {
      "title": "Galeria",
      "new": "Nova foto",
      "empty": "Nenhuma foto ainda. Adicione a primeira.",
      "edit": "Editar",
      "delete": "Excluir",
      "cancel": "Cancelar",
      "editTitle": "Editar foto",
      "statusPublished": "Publicada",
      "statusDraft": "Rascunho",
      "sectionBasics": "Informações básicas",
      "sectionContent": "Conteúdo ({locale})",
      "sectionVisibility": "Visibilidade",
      "image": "Foto",
      "imageHint": "Foto do salão, da fachada, do ambiente ou de um prato.",
      "caption": "Legenda (opcional)",
      "captionHint": "Texto curto exibido sob a foto. Pode ficar em branco.",
      "order": "Ordem",
      "orderHint": "Números menores aparecem primeiro.",
      "published": "Publicada (visível no site)",
      "save": "Salvar",
      "saving": "Salvando…",
      "deleteConfirm": "Apagar esta foto?",
      "errorUnknown": "Não foi possível salvar. Tente de novo."
    },
```

E em `admin.nav`, após `cardapio`:

```json
    "galeria": "Galeria",
```

- [ ] **Step 2: Acrescentar ao menu lateral**

Em `src/components/admin/admin-nav.tsx`, na lista `items`, logo após o cardápio:

```tsx
  { href: "/admin/galeria", key: "galeria", icon: Images, exact: false },
```

E incluir `Images` no import de `lucide-react`.

- [ ] **Step 3: Criar o formulário**

Criar `src/components/admin/gallery-photo-form.tsx`, espelhando `menu-category-form.tsx`. Assinatura:

```tsx
export function GalleryPhotoForm({
  mode,
  photoId,
  defaultValues,
}: {
  mode: "create" | "edit";
  photoId?: string;
  defaultValues: GalleryPhotoFormValues;
})
```

Campos: `image` via `ImageUploadField` com **`preset="cover"`** e `hint={t("imageHint")}` (a assinatura é `{ id?, label?, hint?, preset, value, onChange }`; veja como `menu-item-form.tsx` a liga com `watch`/`setValue`), `caption` (texto localizado, opcional, com `t("captionHint")`), `order` (número), `published` (checkbox).

No envio: `photoFormToInput(values)` e então `createGalleryPhoto(input)` ou `updateGalleryPhoto(photoId!, input)`. Em qualquer erro, `t("errorUnknown")`. No sucesso, `router.push("/admin/galeria")`.

- [ ] **Step 4: Criar o botão de exclusão**

Criar `src/components/admin/gallery-photo-delete-button.tsx`, espelhando `menu-category-delete-button.tsx`, chamando `deleteGalleryPhoto(id)` e confirmando com `t("deleteConfirm")`. Diferente da categoria do cardápio, **não há cascata para avisar** — apagar uma foto apaga só ela.

- [ ] **Step 5: Criar a listagem**

Criar `src/app/[locale]/admin/(dashboard)/galeria/page.tsx`, espelhando a listagem do cardápio. Lê `getAdminGalleryPhotos()` e renderiza uma grade de miniaturas — cada uma com a foto, a legenda (ou nada, se vazia), o badge de publicação e os botões editar/excluir. Um botão "Nova foto" no topo aponta para `/admin/galeria/new`. Vazio → `t("empty")`.

Use `next/image` para as miniaturas, com `width`/`height` explícitos. As fotos vêm do Supabase Storage; o host já está liberado em `next.config.ts` porque os outros recursos usam o mesmo upload.

- [ ] **Step 6: Criar as páginas de nova e edição**

`galeria/new/page.tsx` espelha `cardapio/categorias/new/page.tsx`, usando `emptyGalleryPhotoForm()`. `galeria/[id]/page.tsx` espelha `cardapio/categorias/[id]/page.tsx`, usando `getGalleryPhotoById(id)`, `notFound()` se nulo, e `photoToForm(photo)`.

- [ ] **Step 7: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0, e as rotas `/[locale]/admin/galeria`, `/[locale]/admin/galeria/new` e `/[locale]/admin/galeria/[id]` na listagem do build.

- [ ] **Step 8: Conferir no navegador**

```bash
npm run start
```

Entrar em `http://localhost:3000/pt/admin/login` com `admin@example.com` / `changeme123`, abrir **Galeria**, criar uma foto (cole uma URL de imagem qualquer), editar a legenda, apagar. Encerrar o servidor.

⚠️ **Ao escrever script de navegador, nunca use `page.click('button[type="submit"]')` sem escopo** — o painel tem botões "Sair" que casam com esse seletor, e acertar um deles parece perda de sessão. Isso já custou tempo em duas tarefas do PR anterior. Mire pelo nome acessível: `getByRole("button", { name: "Salvar" })`.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "CRE: CRUD da galeria no admin

Rota nova em portugues (/admin/galeria). /admin/projects continua existindo
ate o PR seguinte — os dois convivem por um PR, do mesmo jeito que
/admin/services conviveu com /admin/cardapio, para nenhum PR intermediario
deixar o site quebrado.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Site público consome as fotos

**Files:**
- Create: `src/components/gallery-photo-card.tsx`
- Modify: `src/app/[locale]/(marketing)/galeria/page.tsx`
- Delete: `src/app/[locale]/(marketing)/galeria/[slug]/page.tsx`
- Modify: `src/components/sections/portfolio-preview.tsx`
- Modify: `src/app/sitemap.ts`
- Modify: `src/app/llms.txt/route.ts`
- Modify: `src/app/llms-full.txt/route.ts`
- Modify: `src/messages/pt.json`

**Interfaces:**
- Consumes: `getGalleryPhotos`, `GalleryPhotoView` (Task 3).
- Produces: `/galeria` renderizando fotos. Fim do plano.

⚠️ **As três superfícies de URL.** `sitemap.ts`, `llms.txt` e `llms-full.txt` todas emitem `/galeria/<slug>` a partir de `Project`. A rota some nesta tarefa, então **as três** têm de ser corrigidas. No PR anterior o plano nomeou só o sitemap e as outras duas ficaram publicando 404s até a revisão final pegar. Não repita.

- [ ] **Step 1: Adicionar as chaves públicas**

Em `src/messages/pt.json`, no namespace `portfolio` (que a página já usa), garantir que exista:

```json
      "empty": "Em breve, novas fotos por aqui.",
```

Mantenha o nome do namespace `portfolio` — a renomeação para `galeria` acontece no PR de renomeações, junto com `about`→`experiencia` e `services`→`cardapio`.

- [ ] **Step 2: Criar o card**

Criar `src/components/gallery-photo-card.tsx`:

```tsx
import Image from "next/image";
import type { GalleryPhotoView } from "@/lib/queries";

/** Uma foto da galeria. A legenda é opcional e some quando vazia. */
export function GalleryPhotoCard({ photo }: { photo: GalleryPhotoView }) {
  return (
    <figure className="flex flex-col gap-2">
      <Image
        src={photo.image}
        alt={photo.caption || ""}
        width={640}
        height={480}
        className="aspect-[4/3] w-full rounded-xl object-cover"
      />
      {photo.caption ? (
        <figcaption className="text-sm text-muted-foreground">
          {photo.caption}
        </figcaption>
      ) : null}
    </figure>
  );
}
```

`alt={photo.caption || ""}` é deliberado: quando há legenda ela descreve a foto; quando não há, um `alt` vazio é o correto para uma imagem puramente decorativa, e é melhor do que inventar texto.

- [ ] **Step 3: Trocar a página da galeria**

Em `src/app/[locale]/(marketing)/galeria/page.tsx`, trocar `getProjects(locale)` por `getGalleryPhotos(locale)`, o `ProjectCard` pelo `GalleryPhotoCard`, e o import correspondente. Manter `PageHeader`, `Section`, `Reveal`, `generateMetadata` e o estado vazio exatamente como estão.

- [ ] **Step 4: Remover a página de detalhe**

```bash
git rm "src/app/[locale]/(marketing)/galeria/[slug]/page.tsx"
```

- [ ] **Step 5: Trocar a prévia da home**

Em `src/components/sections/portfolio-preview.tsx`, ler `getGalleryPhotos(locale)` no lugar de `getProjects(locale)` e renderizar `GalleryPhotoCard`. Manter as chaves de tradução (`home.portfolio`) e o enquadramento visual. Se a lista vier vazia, manter o comportamento atual da seção (retornar `null`).

- [ ] **Step 6: Limpar o sitemap**

Em `src/app/sitemap.ts`, remover o bloco `projectEntries` e a chamada a `getProjectSitemapEntries`, além do import. Manter a entrada de `/galeria`.

- [ ] **Step 7: Corrigir `llms.txt`**

Em `src/app/llms.txt/route.ts`, o bloco que lista projetos emite `/galeria/${x.slug}`. Trocar por uma única linha apontando para `/galeria` (a galeria não tem mais subpáginas), ou remover o bloco de projetos se a linha da própria `/galeria` já existir entre as páginas principais. Remover os imports que ficarem sem uso.

- [ ] **Step 8: Corrigir `llms-full.txt`**

Em `src/app/llms-full.txt/route.ts`, mesma correção: o bloco de projetos emite `/galeria/${s.slug}` e publica o conteúdo longo de cada um. Uma galeria de fotos não tem conteúdo longo — remova o bloco e os imports que ficarem órfãos.

- [ ] **Step 9: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
npx playwright test
```

Esperado: exit 0. O build **não** deve listar `/[locale]/galeria/[slug]`.

Depois, com o servidor rodando, confirmar que nenhuma das duas rotas de texto ainda publica uma subpágina de galeria:

```bash
npm run start
curl -s http://localhost:3000/llms.txt | grep -E "/galeria/[a-z]"
curl -s http://localhost:3000/llms-full.txt | grep -E "/galeria/[a-z]"
```

Esperado: **nenhuma saída** nos dois greps. Encerrar o servidor.

- [ ] **Step 10: Conferir no navegador**

Com o servidor rodando, cadastrar duas ou três fotos pelo admin, abrir **`/pt/galeria`** e confirmar que aparecem na ordem definida, com as legendas, e que a home mostra a prévia. Encerrar o servidor.

⚠️ Se for usar script de navegador, mire os botões pelo nome acessível (`getByRole("button", { name: "Salvar" })`) — `button[type="submit"]` casa com o "Sair" do painel.

- [ ] **Step 11: Commit**

```bash
git add -A
git commit -m "CRE: /galeria passa a renderizar as fotos

A pagina e a previa da home deixam de ler Project e passam a ler
GalleryPhoto. Project continua no schema e no admin — sai no PR seguinte,
para nenhum PR intermediario deixar o site publico quebrado.

/galeria/[slug] sai agora: foto nao tem pagina propria.

As TRES superficies que publicavam /galeria/<slug> foram corrigidas juntas —
sitemap, llms.txt e llms-full.txt. No PR do cardapio so o sitemap estava no
plano e as outras duas ficaram anunciando 404 ate a revisao final pegar.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Critérios de aceite do PR

- `npm run typecheck && npm run lint && npm run build` verde.
- `npm test` verde, com 5 arquivos (`contact-form`, `phone`, `rate-limit`, `menu-form`, `gallery-form`).
- `npx playwright test` verde.
- Banco com 13 models / 14 tabelas.
- É possível cadastrar fotos em `/admin/galeria` e elas aparecem em `/galeria` e na prévia da home.
- `curl` em `/llms.txt` e `/llms-full.txt` não retorna nenhuma URL `/galeria/<slug>`.
- O build não lista `/[locale]/galeria/[slug]`.
- `Service`, `Client`, `Stat`, `TeamMember` e `Project` continuam intactos — a remoção é o PR seguinte.
- Nenhum `git push`.
