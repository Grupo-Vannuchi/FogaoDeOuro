# Cardápio (`MenuCategory` + `MenuItem`) — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao restaurante um cardápio de verdade — categorias e pratos cadastráveis pelo admin, renderizados em `/gastronomia` — no lugar dos seis blocos de marketing herdados da agência.

**Architecture:** Segue **exatamente** o padrão dos recursos existentes: models Prisma com JSON localizado, DAL em `src/lib/queries.ts` com `unstable_cache` + tags, `updateTag` nas escritas, validação `zod` como boundary do servidor, e view-models — nunca linha crua do Prisma para o cliente. A novidade estrutural é a **relação pai/filho** (categoria → itens), a primeira do admin. `Service` **não** é removido aqui: `/gastronomia` passa a ler o cardápio e o model fica órfão até o PR seguinte, para nenhum PR deixar o site público quebrado.

**Tech Stack:** Next 16 (App Router + Turbopack), React 19 + React Compiler, Prisma 6 + PostgreSQL, next-intl (só `pt`), react-hook-form + zod, Vitest, Tailwind 4.

## Global Constraints

- **Branch:** `Development`. **Nunca `git push`** — o dono do projeto envia manualmente.
- **Validação obrigatória** ao fim de cada tarefa: `npm run typecheck && npm run lint && npm run build && npm test`.
- **Banco local de pé** para o build e para `prisma migrate dev`: `docker compose up -d` (container `n8x-marketing-db`, porta 5433).
- 🛑 **Nunca** rodar `prisma migrate reset`, nunca definir `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION`, nunca aceitar prompt de reset. Se o Prisma pedir reset, **pare e reporte BLOCKED**.
- **Nunca editar migração já aplicada** (quebra de checksum).
- **SEM PREÇO.** `MenuItem` não tem campo de preço, nem no schema, nem no admin, nem na página. É diretriz do cliente. Também não emitir `priceRange` no structured data.
- **Português apenas.** Toda string de UI em `src/messages/pt.json`; não existe `en.json`.
- **Nunca devolver linha crua do Prisma ao cliente** — sempre view-model.
- **Toda action de admin** começa com `getCurrentUser()`; sem usuário, retorna `unauthorized`.
- **Toda escrita** chama `updateTag(tags.menu)`.
- Commits convencionais terminando com `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Prefixo do board: **CRE** (novo do zero).

---

## Mapa de arquivos

**Criados:**

| Arquivo | Responsabilidade |
|---|---|
| `src/lib/validations/menu.ts` | schemas zod de categoria e item |
| `src/lib/menu-form.ts` | ponte entre os valores planos do formulário e o input estruturado |
| `src/app/actions/menu.ts` | server actions de CRUD (categoria + item) |
| `src/components/admin/menu-category-form.tsx` | formulário de categoria |
| `src/components/admin/menu-item-form.tsx` | formulário de item |
| `src/components/admin/menu-category-delete-button.tsx` | exclusão de categoria |
| `src/components/admin/menu-item-delete-button.tsx` | exclusão de item |
| `src/components/menu-item-card.tsx` | card público do prato |
| `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx` | listagem agrupada por categoria |
| `src/app/[locale]/admin/(dashboard)/cardapio/categorias/new/page.tsx` | nova categoria |
| `src/app/[locale]/admin/(dashboard)/cardapio/categorias/[id]/page.tsx` | editar categoria |
| `src/app/[locale]/admin/(dashboard)/cardapio/itens/new/page.tsx` | novo item |
| `src/app/[locale]/admin/(dashboard)/cardapio/itens/[id]/page.tsx` | editar item |
| `test/menu-form.test.ts` | testes do mapeamento formulário ↔ input |

**Editados:**

| Arquivo | O quê |
|---|---|
| `prisma/schema.prisma` | + `MenuCategory`, `MenuItem` |
| `src/lib/cache.ts` | + tag `menu` |
| `src/lib/queries.ts` | + `getMenu`, `getMenuCategoryLinks` e os view-models |
| `src/lib/admin-queries.ts` | + queries de admin do cardápio |
| `src/components/admin/admin-nav.tsx` | + item "Cardápio" |
| `src/app/[locale]/(marketing)/gastronomia/page.tsx` | passa a ler o cardápio |
| `src/app/[locale]/(marketing)/layout.tsx` | dropdown do header lê categorias |
| `src/components/layout/header.tsx` | href do dropdown vira âncora (`#slug`) |
| `src/components/sections/services.tsx` | seção da home lê o cardápio |
| `src/app/sitemap.ts` | perde as entradas de `Service` |
| `src/messages/pt.json` | namespaces `cardapio` e `admin.cardapio` |

**Removidos:** `src/app/[locale]/(marketing)/gastronomia/[slug]/page.tsx` — prato não tem página própria.

---

### Task 1: Models e migração

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `prisma/migrations/<timestamp>_add_menu/migration.sql` (gerada pelo Prisma)

**Interfaces:**
- Consumes: nada.
- Produces: os models `MenuCategory` e `MenuItem`. O client Prisma passa a expor `prisma.menuCategory` e `prisma.menuItem`. Campos exatos abaixo — as tarefas seguintes dependem destes nomes.

- [ ] **Step 1: Confirmar que o banco local está de pé**

```bash
docker compose up -d
docker ps --filter "name=n8x-marketing-db" --format "{{.Status}}"
```

Esperado: `Up ... (healthy)`.

- [ ] **Step 2: Adicionar os models ao schema**

Em `prisma/schema.prisma`, na seção de conteúdo (junto de `Information`, antes da seção `// Leads`):

```prisma
/// Uma categoria do cardápio (entradas, pratos, sobremesas, bebidas).
model MenuCategory {
  id          String     @id @default(cuid())
  slug        String     @unique
  name        Json // LocalizedText
  description Json       @default("{}") // LocalizedText, opcional
  order       Int        @default(0)
  published   Boolean    @default(true)
  items       MenuItem[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  @@index([published, order])
  @@map("menu_categories")
}

/// Um prato. SEM preço: a diretriz do cliente proíbe publicar valores, e o
/// buffet é por quilo — preço por prato não existe como conceito aqui.
model MenuItem {
  id          String       @id @default(cuid())
  slug        String       @unique
  categoryId  String
  category    MenuCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  name        Json // LocalizedText
  description Json         @default("{}") // LocalizedText
  image       String       @default("")
  available   Boolean      @default(true)
  order       Int          @default(0)
  tags        String[]     @default([]) // "vegetariano", "picante", …
  /// "A Semana no Fogão": prato fixo de um dia útil, 1 (segunda) a 5 (sexta).
  /// Null = item permanente. Evita um model dedicado só para os cinco pratos.
  weekday     Int?
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  @@index([categoryId, order])
  @@index([available, order])
  @@map("menu_items")
}
```

`onDelete: Cascade` é deliberado: apagar uma categoria apaga seus itens. O formulário de exclusão precisa avisar disso — ver Task 5.

- [ ] **Step 3: Gerar e aplicar a migração**

```bash
npx prisma migrate dev --name add_menu
```

Esperado: cria `prisma/migrations/<timestamp>_add_menu/` e aplica. 🛑 Se pedir reset, **pare e reporte BLOCKED**.

- [ ] **Step 4: Conferir o resultado**

```bash
docker exec n8x-marketing-db psql -U agency -d agency -c "\dt"
```

Esperado: 13 tabelas — as 11 anteriores mais `menu_categories` e `menu_items`.

- [ ] **Step 5: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0 em todos.

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "CRE: models do cardapio (MenuCategory + MenuItem)

Sem campo de preco: a diretriz do cliente proibe publicar valores e o
buffet e por quilo, entao preco por prato nao existe como conceito.

weekday (1-5) cobre 'A Semana no Fogao' — os cinco pratos fixos por dia
util viram uma categoria como outra qualquer, sem model dedicado.

onDelete: Cascade em MenuItem.category: apagar a categoria leva os itens.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Validação e ponte do formulário (TDD)

Esta é a única parte puramente lógica do PR, e a única com testes unitários de verdade. Escreva os testes **antes** da implementação.

**Files:**
- Create: `src/lib/validations/menu.ts`
- Create: `src/lib/menu-form.ts`
- Test: `test/menu-form.test.ts`

**Interfaces:**
- Consumes: nada de tarefas anteriores.
- Produces:
  - `menuCategorySchema`, `type MenuCategoryInput`
  - `menuItemSchema`, `type MenuItemInput`
  - `MenuCategoryFormValues`, `emptyMenuCategoryForm()`, `categoryToForm(row)`, `categoryFormToInput(values)`
  - `MenuItemFormValues`, `emptyMenuItemForm(categoryId)`, `itemToForm(row)`, `itemFormToInput(values)`

- [ ] **Step 1: Escrever os testes que falham**

Criar `test/menu-form.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  emptyMenuCategoryForm,
  categoryFormToInput,
  emptyMenuItemForm,
  itemFormToInput,
  itemToForm,
} from "@/lib/menu-form";
import { menuCategorySchema, menuItemSchema } from "@/lib/validations/menu";

describe("categoria do cardápio", () => {
  it("o formulário vazio não passa na validação (nome é obrigatório)", () => {
    const input = categoryFormToInput(emptyMenuCategoryForm());
    expect(menuCategorySchema.safeParse(input).success).toBe(false);
  });

  it("preenchido, passa e converte a ordem para número", () => {
    const values = emptyMenuCategoryForm();
    values.slug = "entradas";
    values.name.pt = "Entradas";
    values.order = "3";
    const input = categoryFormToInput(values);
    expect(input.order).toBe(3);
    expect(menuCategorySchema.safeParse(input).success).toBe(true);
  });

  it("apara espaços do nome e do slug", () => {
    const values = emptyMenuCategoryForm();
    values.slug = "  sobremesas  ";
    values.name.pt = "  Sobremesas  ";
    const input = categoryFormToInput(values);
    expect(input.slug).toBe("sobremesas");
    expect(input.name.pt).toBe("Sobremesas");
  });
});

describe("item do cardápio", () => {
  it("o formulário vazio não passa (nome e categoria obrigatórios)", () => {
    const input = itemFormToInput(emptyMenuItemForm(""));
    expect(menuItemSchema.safeParse(input).success).toBe(false);
  });

  it("preenchido, passa na validação", () => {
    const values = emptyMenuItemForm("cat_1");
    values.slug = "picanha";
    values.name.pt = "Picanha na brasa";
    const input = itemFormToInput(values);
    expect(input.categoryId).toBe("cat_1");
    expect(menuItemSchema.safeParse(input).success).toBe(true);
  });

  it("weekday vazio vira null; preenchido vira número", () => {
    const values = emptyMenuItemForm("cat_1");
    values.slug = "feijoada";
    values.name.pt = "Feijoada";

    expect(itemFormToInput(values).weekday).toBeNull();

    values.weekday = "3";
    expect(itemFormToInput(values).weekday).toBe(3);
  });

  it("recusa weekday fora de 1–5 (o restaurante abre de segunda a sexta)", () => {
    const values = emptyMenuItemForm("cat_1");
    values.slug = "x";
    values.name.pt = "X";
    values.weekday = "6";
    expect(menuItemSchema.safeParse(itemFormToInput(values)).success).toBe(false);
  });

  it("separa as tags por vírgula, aparando e descartando vazias", () => {
    const values = emptyMenuItemForm("cat_1");
    values.slug = "salada";
    values.name.pt = "Salada";
    values.tags = " vegetariano , , picante ";
    expect(itemFormToInput(values).tags).toEqual(["vegetariano", "picante"]);
  });

  it("itemToForm devolve as tags como texto separado por vírgula", () => {
    const form = itemToForm({
      slug: "salada",
      categoryId: "cat_1",
      name: { pt: "Salada" },
      description: { pt: "" },
      image: "",
      available: true,
      order: 0,
      tags: ["vegetariano", "leve"],
      weekday: null,
    });
    expect(form.tags).toBe("vegetariano, leve");
    expect(form.weekday).toBe("");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npx vitest run test/menu-form.test.ts
```

Esperado: FALHA com "Failed to resolve import @/lib/menu-form".

- [ ] **Step 3: Criar os schemas de validação**

Criar `src/lib/validations/menu.ts`:

```ts
import { z } from "zod";
import { locales, defaultLocale, type Locale } from "@/i18n/routing";

/**
 * Validação do editor de cardápio. Categoria e item.
 *
 * ⚠️ Não existe campo de preço, de propósito: a diretriz do cliente proíbe
 * publicar valores e o buffet é por quilo. Não adicione um.
 *
 * O formulário do cliente coleta valores planos e os mapeia para estas formas
 * antes de enviar; a server action revalida com o mesmo schema, como boundary.
 */

/** Constrói um validador `{ pt }`: o locale padrão é obrigatório, os demais não. */
function localizedText(max: number) {
  return z.object(
    Object.fromEntries(
      locales.map((l) => [
        l,
        l === defaultLocale
          ? z.string().trim().min(1, "Required").max(max)
          : z.string().trim().max(max),
      ]),
    ) as Record<Locale, z.ZodString>,
  );
}

/** Opcional em todos os locales — usado por descrições. */
function optionalLocalizedText(max: number) {
  return z.object(
    Object.fromEntries(
      locales.map((l) => [l, z.string().trim().max(max)]),
    ) as Record<Locale, z.ZodString>,
  );
}

const slug = z
  .string()
  .trim()
  .min(1)
  .max(80)
  .regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, "Use minúsculas, números e hífens");

const url = z.string().trim().url().max(500);

export const menuCategorySchema = z.object({
  slug,
  name: localizedText(80),
  description: optionalLocalizedText(300),
  order: z.coerce.number().int().min(0).max(9999),
  published: z.boolean(),
});

export const menuItemSchema = z.object({
  slug,
  categoryId: z.string().trim().min(1),
  name: localizedText(120),
  description: optionalLocalizedText(600),
  image: z.union([url, z.literal("")]),
  available: z.boolean(),
  order: z.coerce.number().int().min(0).max(9999),
  tags: z.array(z.string().trim().min(1).max(40)).max(10),
  /// 1 = segunda … 5 = sexta. O restaurante não abre no fim de semana.
  weekday: z.union([z.coerce.number().int().min(1).max(5), z.null()]),
});

export type MenuCategoryInput = z.infer<typeof menuCategorySchema>;
export type MenuItemInput = z.infer<typeof menuItemSchema>;
```

- [ ] **Step 4: Criar a ponte do formulário**

Criar `src/lib/menu-form.ts`:

```ts
import { locales, type Locale } from "@/i18n/routing";
import type { MenuCategoryInput, MenuItemInput } from "@/lib/validations/menu";

/**
 * Ponte entre os formulários do cardápio e a forma armazenada (números e listas
 * como texto, mapas localizados). Sem "use client" nem "server-only" — os dois
 * lados importam este módulo.
 */

type LocalizedStrings = Record<Locale, string>;

function blankLocalized(): LocalizedStrings {
  return Object.fromEntries(locales.map((l) => [l, ""])) as LocalizedStrings;
}

function readLocalized(value: unknown): LocalizedStrings {
  const obj = (value ?? {}) as Record<string, unknown>;
  return Object.fromEntries(
    locales.map((l) => [l, typeof obj[l] === "string" ? (obj[l] as string) : ""]),
  ) as LocalizedStrings;
}

function trimLocalized(value: LocalizedStrings): LocalizedStrings {
  return Object.fromEntries(
    locales.map((l) => [l, value[l].trim()]),
  ) as LocalizedStrings;
}

// --- Categoria ---------------------------------------------------------------

export type MenuCategoryFormValues = {
  slug: string;
  name: LocalizedStrings;
  description: LocalizedStrings;
  order: string;
  published: boolean;
};

export function emptyMenuCategoryForm(): MenuCategoryFormValues {
  return {
    slug: "",
    name: blankLocalized(),
    description: blankLocalized(),
    order: "0",
    published: true,
  };
}

type MenuCategoryRow = {
  slug: string;
  name: unknown;
  description: unknown;
  order: number;
  published: boolean;
};

export function categoryToForm(c: MenuCategoryRow): MenuCategoryFormValues {
  return {
    slug: c.slug,
    name: readLocalized(c.name),
    description: readLocalized(c.description),
    order: String(c.order),
    published: c.published,
  };
}

export function categoryFormToInput(
  values: MenuCategoryFormValues,
): MenuCategoryInput {
  return {
    slug: values.slug.trim(),
    name: trimLocalized(values.name),
    description: trimLocalized(values.description),
    order: Number(values.order),
    published: values.published,
  };
}

// --- Item --------------------------------------------------------------------

export type MenuItemFormValues = {
  slug: string;
  categoryId: string;
  name: LocalizedStrings;
  description: LocalizedStrings;
  image: string;
  available: boolean;
  order: string;
  /** Lista separada por vírgula no formulário; array no banco. */
  tags: string;
  /** "" = prato permanente; "1".."5" = dia útil fixo. */
  weekday: string;
};

export function emptyMenuItemForm(categoryId: string): MenuItemFormValues {
  return {
    slug: "",
    categoryId,
    name: blankLocalized(),
    description: blankLocalized(),
    image: "",
    available: true,
    order: "0",
    tags: "",
    weekday: "",
  };
}

type MenuItemRow = {
  slug: string;
  categoryId: string;
  name: unknown;
  description: unknown;
  image: string;
  available: boolean;
  order: number;
  tags: string[];
  weekday: number | null;
};

export function itemToForm(i: MenuItemRow): MenuItemFormValues {
  return {
    slug: i.slug,
    categoryId: i.categoryId,
    name: readLocalized(i.name),
    description: readLocalized(i.description),
    image: i.image,
    available: i.available,
    order: String(i.order),
    tags: i.tags.join(", "),
    weekday: i.weekday === null ? "" : String(i.weekday),
  };
}

export function itemFormToInput(values: MenuItemFormValues): MenuItemInput {
  return {
    slug: values.slug.trim(),
    categoryId: values.categoryId.trim(),
    name: trimLocalized(values.name),
    description: trimLocalized(values.description),
    image: values.image.trim(),
    available: values.available,
    order: Number(values.order),
    tags: values.tags
      .split(",")
      .map((t) => t.trim())
      .filter(Boolean),
    weekday: values.weekday.trim() === "" ? null : Number(values.weekday),
  };
}
```

- [ ] **Step 5: Rodar os testes**

```bash
npx vitest run test/menu-form.test.ts
```

Esperado: PASS, 8 testes.

- [ ] **Step 6: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0; `Test Files 4 passed (4)`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/validations/menu.ts src/lib/menu-form.ts test/menu-form.test.ts
git commit -m "CRE: validacao e ponte de formulario do cardapio

Schemas zod de categoria e item, mais o mapeamento entre os valores planos
do formulario e o input estruturado que a server action revalida.

Escrito por TDD: os testes cobrem o que da errado calado — weekday vazio
virando null em vez de NaN, tags separadas por virgula com entradas vazias
descartadas, e a recusa de weekday fora de 1-5 (a casa abre de segunda a
sexta).

Sem campo de preco, por diretriz do cliente.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Camada de acesso a dados

**Files:**
- Modify: `src/lib/cache.ts`
- Modify: `src/lib/queries.ts`
- Modify: `src/lib/admin-queries.ts`

**Interfaces:**
- Consumes: os models da Task 1.
- Produces:
  - `tags.menu` (string `"menu"`)
  - `getMenu(locale): Promise<MenuCategoryView[]>` — categorias publicadas com seus itens disponíveis, ordenadas
  - `getMenuCategoryLinks(locale): Promise<{ slug: string; name: string }[]>` — para o dropdown do header
  - `MenuCategoryView`, `MenuItemView`
  - `getAdminMenu(): Promise<…>`, `getMenuCategoryById(id)`, `getMenuItemById(id)`, `getMenuCategoryOptions()`

- [ ] **Step 1: Adicionar a tag de cache**

Em `src/lib/cache.ts`, dentro de `tags`, após `clients`:

```ts
  /** Cardápio: categorias e itens compartilham uma tag só. */
  menu: "menu",
```

- [ ] **Step 2: Adicionar os view-models e as queries públicas**

Em `src/lib/queries.ts`, junto dos outros tipos exportados no topo:

```ts
export type MenuItemView = {
  id: string;
  slug: string;
  name: string;
  description: string;
  image: string;
  tags: string[];
  /** 1 (segunda) a 5 (sexta); null = prato permanente. */
  weekday: number | null;
};

export type MenuCategoryView = {
  id: string;
  slug: string;
  name: string;
  description: string;
  items: MenuItemView[];
};
```

E, junto das demais funções cacheadas:

```ts
/** O cardápio publicado: categorias na ordem, cada uma com os itens disponíveis.
 * Uma consulta só — os itens vêm no `include`, sem N+1. */
export const getMenu = unstable_cache(
  async (locale: Locale): Promise<MenuCategoryView[]> => {
    const rows = await prisma.menuCategory.findMany({
      where: { published: true },
      orderBy: { order: "asc" },
      include: {
        items: {
          where: { available: true },
          orderBy: { order: "asc" },
        },
      },
    });
    return rows.map((c) => ({
      id: c.id,
      slug: c.slug,
      name: localize(c.name, locale),
      description: localize(c.description, locale),
      items: c.items.map((i) => ({
        id: i.id,
        slug: i.slug,
        name: localize(i.name, locale),
        description: localize(i.description, locale),
        image: i.image,
        tags: i.tags,
        weekday: i.weekday,
      })),
    }));
  },
  ["menu"],
  { tags: [tags.menu], revalidate },
);

/** Só o que o dropdown de "Nossa Gastronomia" precisa. */
export const getMenuCategoryLinks = unstable_cache(
  async (locale: Locale): Promise<{ slug: string; name: string }[]> => {
    const rows = await prisma.menuCategory.findMany({
      where: { published: true },
      orderBy: { order: "asc" },
      select: { slug: true, name: true },
    });
    return rows.map((c) => ({ slug: c.slug, name: localize(c.name, locale) }));
  },
  ["menu", "links"],
  { tags: [tags.menu], revalidate },
);
```

- [ ] **Step 3: Adicionar as queries do admin**

Em `src/lib/admin-queries.ts`, seguindo o padrão das funções vizinhas (adicione `MenuCategory` e `MenuItem` ao import de tipos do `@prisma/client`):

```ts
/** Cardápio completo para o admin: todas as categorias, publicadas ou não,
 * com todos os seus itens. */
export async function getAdminMenu(): Promise<
  (MenuCategory & { items: MenuItem[] })[]
> {
  return prisma.menuCategory.findMany({
    orderBy: { order: "asc" },
    include: { items: { orderBy: { order: "asc" } } },
  });
}

export async function getMenuCategoryById(
  id: string,
): Promise<MenuCategory | null> {
  return prisma.menuCategory.findUnique({ where: { id } });
}

export async function getMenuItemById(id: string): Promise<MenuItem | null> {
  return prisma.menuItem.findUnique({ where: { id } });
}

/** Categorias reduzidas ao que o seletor do formulário de item precisa. */
export async function getMenuCategoryOptions(): Promise<
  { id: string; name: unknown }[]
> {
  return prisma.menuCategory.findMany({
    orderBy: { order: "asc" },
    select: { id: true, name: true },
  });
}
```

- [ ] **Step 4: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0 em todos.

- [ ] **Step 5: Commit**

```bash
git add src/lib/cache.ts src/lib/queries.ts src/lib/admin-queries.ts
git commit -m "CRE: DAL do cardapio

getMenu traz categorias e itens numa consulta so, via include, para nao
gerar N+1 quando a pagina renderizar. Categorias e itens compartilham a tag
de cache 'menu': uma escrita em qualquer um dos dois invalida a pagina
inteira, que e sempre lida junto.

getMenuCategoryLinks existe separada porque o dropdown do header so precisa
de slug e nome — nao vale carregar os itens em toda pagina do site.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Server actions

**Files:**
- Create: `src/app/actions/menu.ts`

**Interfaces:**
- Consumes: `menuCategorySchema`, `menuItemSchema`, `MenuCategoryInput`, `MenuItemInput` (Task 2); `tags.menu` (Task 3).
- Produces: `createMenuCategory`, `updateMenuCategory`, `deleteMenuCategory`, `createMenuItem`, `updateMenuItem`, `deleteMenuItem`, e o tipo `MenuActionResult`.

- [ ] **Step 1: Criar as actions**

Criar `src/app/actions/menu.ts`:

```ts
"use server";

import { updateTag } from "next/cache";
import { prisma } from "@/lib/prisma";
import { getCurrentUser } from "@/lib/auth";
import { tags } from "@/lib/cache";
import {
  menuCategorySchema,
  menuItemSchema,
  type MenuCategoryInput,
  type MenuItemInput,
} from "@/lib/validations/menu";

export type MenuActionResult =
  | { ok: true; id: string }
  | { ok: false; error: "unauthorized" | "invalid" | "duplicate" | "unknown" };

/** Categorias e itens são sempre lidos juntos, então uma tag só invalida os
 * dois. `updateTag` (e não `revalidateTag`) para o admin ver a própria escrita
 * na visita seguinte, não na outra. */
function revalidateMenu(): void {
  updateTag(tags.menu);
}

/** Violação de unique do Prisma (slug repetido). */
function isUniqueViolation(error: unknown): boolean {
  return (
    typeof error === "object" &&
    error !== null &&
    (error as { code?: string }).code === "P2002"
  );
}

function categoryData(input: MenuCategoryInput) {
  return {
    slug: input.slug,
    name: input.name,
    description: input.description,
    order: input.order,
    published: input.published,
  };
}

function itemData(input: MenuItemInput) {
  return {
    slug: input.slug,
    categoryId: input.categoryId,
    name: input.name,
    description: input.description,
    image: input.image,
    available: input.available,
    order: input.order,
    tags: input.tags,
    weekday: input.weekday,
  };
}

export async function createMenuCategory(
  input: MenuCategoryInput,
): Promise<MenuActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = menuCategorySchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const c = await prisma.menuCategory.create({
      data: categoryData(parsed.data),
    });
    revalidateMenu();
    return { ok: true, id: c.id };
  } catch (error) {
    if (isUniqueViolation(error)) return { ok: false, error: "duplicate" };
    console.error("Failed to create menu category", error);
    return { ok: false, error: "unknown" };
  }
}

export async function updateMenuCategory(
  id: string,
  input: MenuCategoryInput,
): Promise<MenuActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = menuCategorySchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const c = await prisma.menuCategory.update({
      where: { id },
      data: categoryData(parsed.data),
    });
    revalidateMenu();
    return { ok: true, id: c.id };
  } catch (error) {
    if (isUniqueViolation(error)) return { ok: false, error: "duplicate" };
    console.error("Failed to update menu category", error);
    return { ok: false, error: "unknown" };
  }
}

/** Apaga a categoria. `onDelete: Cascade` no schema leva os itens junto — a UI
 * precisa avisar antes de chamar. */
export async function deleteMenuCategory(id: string): Promise<{ ok: boolean }> {
  const user = await getCurrentUser();
  if (!user) return { ok: false };

  try {
    await prisma.menuCategory.delete({ where: { id } });
    revalidateMenu();
    return { ok: true };
  } catch (error) {
    console.error("Failed to delete menu category", error);
    return { ok: false };
  }
}

export async function createMenuItem(
  input: MenuItemInput,
): Promise<MenuActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = menuItemSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const i = await prisma.menuItem.create({ data: itemData(parsed.data) });
    revalidateMenu();
    return { ok: true, id: i.id };
  } catch (error) {
    if (isUniqueViolation(error)) return { ok: false, error: "duplicate" };
    console.error("Failed to create menu item", error);
    return { ok: false, error: "unknown" };
  }
}

export async function updateMenuItem(
  id: string,
  input: MenuItemInput,
): Promise<MenuActionResult> {
  const user = await getCurrentUser();
  if (!user) return { ok: false, error: "unauthorized" };

  const parsed = menuItemSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "invalid" };

  try {
    const i = await prisma.menuItem.update({
      where: { id },
      data: itemData(parsed.data),
    });
    revalidateMenu();
    return { ok: true, id: i.id };
  } catch (error) {
    if (isUniqueViolation(error)) return { ok: false, error: "duplicate" };
    console.error("Failed to update menu item", error);
    return { ok: false, error: "unknown" };
  }
}

export async function deleteMenuItem(id: string): Promise<{ ok: boolean }> {
  const user = await getCurrentUser();
  if (!user) return { ok: false };

  try {
    await prisma.menuItem.delete({ where: { id } });
    revalidateMenu();
    return { ok: true };
  } catch (error) {
    console.error("Failed to delete menu item", error);
    return { ok: false };
  }
}
```

- [ ] **Step 2: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0 em todos.

- [ ] **Step 3: Commit**

```bash
git add src/app/actions/menu.ts
git commit -m "CRE: server actions do cardapio

CRUD de categoria e item. Toda action passa por getCurrentUser() e
revalida o zod no servidor, mesmo o cliente ja tendo validado — o schema e
o boundary, nao a conveniencia do formulario.

Slug duplicado (P2002 do Prisma) volta como 'duplicate' em vez de 'unknown',
para o formulario poder dizer o que houve em vez de 'erro inesperado'.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Admin — categorias

**Files:**
- Read first (referência de padrão): `src/components/admin/testimonial-form.tsx`, `src/components/admin/testimonial-delete-button.tsx`
- Create: `src/components/admin/menu-category-form.tsx`
- Create: `src/components/admin/menu-category-delete-button.tsx`
- Create: `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx`
- Create: `src/app/[locale]/admin/(dashboard)/cardapio/categorias/new/page.tsx`
- Create: `src/app/[locale]/admin/(dashboard)/cardapio/categorias/[id]/page.tsx`
- Modify: `src/components/admin/admin-nav.tsx`
- Modify: `src/messages/pt.json`

**Interfaces:**
- Consumes: `MenuCategoryFormValues`, `emptyMenuCategoryForm`, `categoryToForm`, `categoryFormToInput` (Task 2); `getAdminMenu`, `getMenuCategoryById` (Task 3); `createMenuCategory`, `updateMenuCategory`, `deleteMenuCategory` (Task 4).
- Produces: a rota `/admin/cardapio` e o item "Cardápio" no menu lateral. A Task 6 acrescenta os itens a esta mesma listagem.

**Padrão a seguir:** `src/components/admin/testimonial-form.tsx` é o modelo — leia-o antes de escrever. Reproduza a mesma estrutura (react-hook-form + zodResolver, `mode: "create" | "edit"`, estado de envio, tratamento de erro, `router.push` no sucesso) trocando apenas os campos. Não invente um padrão novo.

Campos do formulário de categoria: `slug` (texto), `name` (texto localizado, obrigatório), `description` (textarea localizada, opcional), `order` (número), `published` (checkbox).

- [ ] **Step 1: Adicionar as chaves de tradução**

Em `src/messages/pt.json`, dentro de `admin`, um namespace novo `cardapio`:

```json
    "cardapio": {
      "title": "Cardápio",
      "newCategory": "Nova categoria",
      "newItem": "Novo prato",
      "empty": "Nenhuma categoria cadastrada ainda.",
      "emptyItems": "Nenhum prato nesta categoria.",
      "edit": "Editar",
      "editCategoryTitle": "Editar {name}",
      "editItemTitle": "Editar {name}",
      "statusPublished": "Publicada",
      "statusDraft": "Rascunho",
      "statusAvailable": "Disponível",
      "statusUnavailable": "Indisponível",
      "categorySlug": "Identificador na URL",
      "categoryName": "Nome",
      "categoryDescription": "Descrição (opcional)",
      "itemName": "Nome do prato",
      "itemDescription": "Descrição",
      "itemImage": "Foto",
      "itemCategory": "Categoria",
      "itemTags": "Etiquetas",
      "itemTagsHint": "Separe por vírgula: vegetariano, picante",
      "itemWeekday": "Dia da semana",
      "itemWeekdayHint": "Só para os pratos fixos de \"A Semana no Fogão\". Deixe em branco se o prato é permanente.",
      "weekdayNone": "Permanente",
      "weekday1": "Segunda",
      "weekday2": "Terça",
      "weekday3": "Quarta",
      "weekday4": "Quinta",
      "weekday5": "Sexta",
      "order": "Ordem",
      "published": "Publicada",
      "available": "Disponível",
      "save": "Salvar",
      "saving": "Salvando…",
      "deleteCategoryConfirm": "Apagar \"{name}\"? Os pratos desta categoria também serão apagados.",
      "deleteItemConfirm": "Apagar \"{name}\"?",
      "errorDuplicate": "Já existe um item com esse identificador na URL.",
      "errorUnknown": "Não foi possível salvar. Tente de novo."
    },
```

E, em `admin.nav`, após `informations`:

```json
    "cardapio": "Cardápio",
```

- [ ] **Step 2: Acrescentar o item ao menu lateral**

Em `src/components/admin/admin-nav.tsx`, adicionar à lista `items`, logo após o dashboard (o cardápio é o recurso principal do restaurante):

```tsx
  { href: "/admin/cardapio", key: "cardapio", icon: UtensilsCrossed, exact: false },
```

E incluir `UtensilsCrossed` no import de `lucide-react`.

- [ ] **Step 3: Criar o formulário de categoria**

Criar `src/components/admin/menu-category-form.tsx`, espelhando `testimonial-form.tsx`. A assinatura do componente:

```tsx
export function MenuCategoryForm({
  mode,
  categoryId,
  defaultValues,
}: {
  mode: "create" | "edit";
  categoryId?: string;
  defaultValues: MenuCategoryFormValues;
})
```

No envio: `categoryFormToInput(values)` e então `createMenuCategory(input)` ou `updateMenuCategory(categoryId!, input)`. Em `{ ok: false, error: "duplicate" }`, mostrar `t("errorDuplicate")`; em qualquer outro erro, `t("errorUnknown")`. No sucesso, `router.push("/admin/cardapio")`.

- [ ] **Step 4: Criar o botão de exclusão**

Criar `src/components/admin/menu-category-delete-button.tsx`, espelhando `testimonial-delete-button.tsx`, chamando `deleteMenuCategory(id)`. A confirmação usa `t("deleteCategoryConfirm", { name })` — **o texto precisa avisar que os pratos vão junto**, porque o `onDelete: Cascade` do schema apaga em cascata.

- [ ] **Step 5: Criar a listagem**

Criar `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx`, espelhando a listagem de depoimentos. Lê `getAdminMenu()` e renderiza **categorias como seções**, cada uma com o seu badge de publicação, os botões de editar/apagar, e a lista de itens dentro (a Task 6 preenche a parte dos itens; nesta tarefa, renderize a categoria e, no lugar dos itens, `t("emptyItems")`). Um botão "Nova categoria" no topo aponta para `/admin/cardapio/categorias/new`.

- [ ] **Step 6: Criar as páginas de nova e edição**

`categorias/new/page.tsx` espelha `testimonials/new/page.tsx`, usando `emptyMenuCategoryForm()`. `categorias/[id]/page.tsx` espelha `testimonials/[id]/page.tsx`, usando `getMenuCategoryById(id)`, `notFound()` se nulo, e `categoryToForm(category)`.

- [ ] **Step 7: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0. O build deve listar as rotas `/[locale]/admin/cardapio`, `/[locale]/admin/cardapio/categorias/new` e `/[locale]/admin/cardapio/categorias/[id]`.

- [ ] **Step 8: Conferir no navegador**

```bash
npm run start
```

Entrar em `http://localhost:3000/pt/admin/login` com `admin@example.com` / `changeme123`, abrir **Cardápio**, criar uma categoria ("Entradas", slug `entradas`), editá-la e apagá-la. Encerrar o servidor.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "CRE: CRUD de categorias do cardapio no admin

Rota nova em portugues (/admin/cardapio) — as rotas antigas em ingles sao
renomeadas num PR posterior; nao ha o que renomear numa rota que nasce
agora.

O texto de confirmacao da exclusao avisa que os pratos vao junto: o schema
tem onDelete Cascade, e apagar uma categoria em silencio levaria o cardapio
inteiro dela.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Admin — pratos

**Files:**
- Read first (referência): `src/components/admin/testimonial-form.tsx`, `src/components/admin/image-upload-field.tsx`
- Create: `src/components/admin/menu-item-form.tsx`
- Create: `src/components/admin/menu-item-delete-button.tsx`
- Create: `src/app/[locale]/admin/(dashboard)/cardapio/itens/new/page.tsx`
- Create: `src/app/[locale]/admin/(dashboard)/cardapio/itens/[id]/page.tsx`
- Modify: `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx` (renderizar os itens)

**Interfaces:**
- Consumes: `MenuItemFormValues`, `emptyMenuItemForm`, `itemToForm`, `itemFormToInput` (Task 2); `getAdminMenu`, `getMenuItemById`, `getMenuCategoryOptions` (Task 3); `createMenuItem`, `updateMenuItem`, `deleteMenuItem` (Task 4); as chaves `admin.cardapio` (Task 5).
- Produces: o CRUD de pratos completo. A Task 7 consome o cardápio no site público.

- [ ] **Step 1: Criar o formulário de prato**

Criar `src/components/admin/menu-item-form.tsx`, espelhando `testimonial-form.tsx`. Assinatura:

```tsx
export function MenuItemForm({
  mode,
  itemId,
  categories,
  defaultValues,
}: {
  mode: "create" | "edit";
  itemId?: string;
  categories: { id: string; name: string }[];
  defaultValues: MenuItemFormValues;
})
```

Campos: `categoryId` (`<select>` alimentado por `categories`), `slug`, `name` (localizado, obrigatório), `description` (textarea localizada), `image`, `tags` (texto, com `t("itemTagsHint")` embaixo), `weekday` (`<select>` com "Permanente" mapeado para `""` e as opções de 1 a 5 usando `t("weekday1")`…`t("weekday5")`), `order`, `available` (checkbox).

Para a foto, usar `ImageUploadField` com **`preset="cover"`** — é o preset das
imagens de capa (`information-form.tsx`, `project-form.tsx`), que é o
enquadramento de um prato num card. `avatar`, `photo` e `logo` são os outros
presets e não servem aqui. A assinatura é
`{ id?, label?, hint?, preset, value, onChange }`.

⚠️ **Não adicionar campo de preço.** É diretriz do cliente.

No envio: `itemFormToInput(values)` e então `createMenuItem` / `updateMenuItem(itemId!, input)`. Mesmo tratamento de `duplicate` da Task 5. No sucesso, `router.push("/admin/cardapio")`.

- [ ] **Step 2: Criar o botão de exclusão do prato**

Criar `src/components/admin/menu-item-delete-button.tsx`, espelhando `testimonial-delete-button.tsx`, chamando `deleteMenuItem(id)` e confirmando com `t("deleteItemConfirm", { name })`.

- [ ] **Step 3: Criar as páginas de novo e edição**

`itens/new/page.tsx`: lê `getMenuCategoryOptions()`, resolve cada `name` com `localize(c.name, locale)` para montar `categories`, e usa `emptyMenuItemForm(searchParams.categoria ?? categories[0]?.id ?? "")` — a listagem passa a categoria por query string, para "adicionar prato" já vir com a categoria certa.

`itens/[id]/page.tsx`: `getMenuItemById(id)`, `notFound()` se nulo, `itemToForm(item)`, e as mesmas `categories`.

- [ ] **Step 4: Renderizar os itens na listagem**

Em `src/app/[locale]/admin/(dashboard)/cardapio/page.tsx`, dentro de cada categoria, listar `category.items` com nome, badge de disponibilidade, o dia da semana quando `weekday` não é nulo, e os botões de editar/apagar. Cada categoria ganha um link "Novo prato" apontando para `/admin/cardapio/itens/new?categoria=<id>`. Quando a categoria não tiver itens, `t("emptyItems")`.

- [ ] **Step 5: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0 em todos.

- [ ] **Step 6: Conferir no navegador**

```bash
npm run start
```

Em `/pt/admin/cardapio`: criar uma categoria, criar dois pratos dentro dela (um permanente, um com `weekday` = Quarta), editar um, apagar o outro. Apagar a categoria e confirmar que os pratos somem junto. Encerrar o servidor.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "CRE: CRUD de pratos do cardapio no admin

O formulario de prato traz o seletor de categoria, a foto (ImageUploadField,
como os demais recursos), as etiquetas separadas por virgula e o seletor de
dia da semana para 'A Semana no Fogao' — vazio significa prato permanente.

Sem campo de preco, por diretriz do cliente.

O botao 'Novo prato' de cada categoria leva o id na query string, para o
formulario ja abrir na categoria certa.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Site público consome o cardápio

`Service` **não é removido aqui** — só deixa de ser lido. O model e o admin de serviços saem no PR seguinte. Assim nenhum PR deixa o site quebrado.

**Files:**
- Create: `src/components/menu-item-card.tsx`
- Modify: `src/app/[locale]/(marketing)/gastronomia/page.tsx`
- Delete: `src/app/[locale]/(marketing)/gastronomia/[slug]/page.tsx`
- Modify: `src/app/[locale]/(marketing)/layout.tsx`
- Modify: `src/components/layout/header.tsx` (href do dropdown vira âncora)
- Modify: `src/components/sections/services.tsx`
- Modify: `src/app/sitemap.ts`
- Modify: `src/messages/pt.json`

**Interfaces:**
- Consumes: `getMenu`, `getMenuCategoryLinks`, `MenuCategoryView`, `MenuItemView` (Task 3).
- Produces: `/gastronomia` renderizando o cardápio. Fim do plano.

- [ ] **Step 1: Adicionar as chaves públicas**

Em `src/messages/pt.json`, dentro do namespace `services` (que a página de gastronomia já usa), acrescentar:

```json
      "weekOfTitle": "A Semana no Fogão",
      "weekday1": "Segunda",
      "weekday2": "Terça",
      "weekday3": "Quarta",
      "weekday4": "Quinta",
      "weekday5": "Sexta",
```

Se `services.empty` ainda não existir, acrescente `"empty": "O cardápio está sendo atualizado."`.

⚠️ **Manter o namespace `services` por enquanto.** Ele ainda se chama assim, e a
renomeação para `cardapio` acontece num PR posterior, junto com as demais
(`about` → `experiencia`, `portfolio` → `galeria`). Renomear aqui espalharia a
mudança por arquivos que este PR não deveria tocar.

- [ ] **Step 2: Criar o card do prato**

Criar `src/components/menu-item-card.tsx` — um server component simples, espelhando o estilo de `src/components/service-card.tsx` (leia-o primeiro):

```tsx
import Image from "next/image";
import type { MenuItemView } from "@/lib/queries";

/** Card de um prato. Sem preço: o cliente não publica valores. */
export function MenuItemCard({ item }: { item: MenuItemView }) {
  return (
    <article className="flex h-full flex-col gap-3 rounded-xl border border-border bg-card p-5">
      {item.image ? (
        <Image
          src={item.image}
          alt=""
          width={480}
          height={320}
          className="h-40 w-full rounded-lg object-cover"
        />
      ) : null}
      <div className="flex flex-col gap-1.5">
        <h3 className="font-serif text-lg font-bold">{item.name}</h3>
        {item.description ? (
          <p className="text-sm text-muted-foreground">{item.description}</p>
        ) : null}
      </div>
      {item.tags.length > 0 ? (
        <ul className="mt-auto flex flex-wrap gap-1.5">
          {item.tags.map((tag) => (
            <li
              key={tag}
              className="rounded-full bg-muted px-2 py-0.5 text-xs text-muted-foreground"
            >
              {tag}
            </li>
          ))}
        </ul>
      ) : null}
    </article>
  );
}
```

- [ ] **Step 3: Trocar a página de gastronomia**

Reescrever `src/app/[locale]/(marketing)/gastronomia/page.tsx` para ler `getMenu(locale)` e renderizar uma seção por categoria — título da categoria, descrição quando houver, e uma grade de `MenuItemCard`. Itens com `weekday` não nulo recebem uma etiqueta com o dia (`t("weekday" + item.weekday)`). Manter `generateMetadata` como está, incluindo `localeMetadata(locale, "/gastronomia")`. Quando `getMenu` devolver lista vazia, `t("empty")`.

- [ ] **Step 4: Remover a página de detalhe do serviço**

```bash
git rm "src/app/[locale]/(marketing)/gastronomia/[slug]/page.tsx"
```

- [ ] **Step 5: Trocar o dropdown do header — duas edições acopladas**

⚠️ O header monta o destino do dropdown como **rota**, não âncora:
`href={\`${item.href}/${link.slug}\`}` (`src/components/layout/header.tsx`, no
menu de desktop **e** no de mobile). Como `/gastronomia/[slug]` deixou de
existir na Step 4, deixar isso como está produz links 404. São duas edições:

**(a)** Em `src/app/[locale]/(marketing)/layout.tsx`, trocar a origem dos links.
Hoje a linha é `const serviceLinks = services.map((s) => ({ slug: s.slug, title: s.title }));`,
alimentada por `getServices(locale)`. Passa a vir de `getMenuCategoryLinks(locale)`,
que devolve `{ slug, name }` — mapeie `name` para `title`, que é o campo do tipo
`DropdownLink`:

```tsx
const categoryLinks = categories.map((c) => ({ slug: c.slug, title: c.name }));
```

Remova `getServices` do import e da chamada em paralelo, se ele não for mais
usado no arquivo.

**(b)** Em `src/components/layout/header.tsx`, trocar a barra por `#` nos **dois**
lugares que montam o href do dropdown:

```tsx
href={`${item.href}#${link.slug}`}
```

Não confundir com o bloco de "Novidades", mais abaixo, que monta
`/informations/${link.slug}` — esse é um caminho de verdade e **fica como está**.

Para a âncora funcionar, a Step 3 precisa ter posto `id={category.slug}` na
seção de cada categoria.

- [ ] **Step 6: Trocar a seção da home**

Em `src/components/sections/services.tsx`, ler `getMenu(locale)` no lugar de `getServices(locale)` e renderizar uma prévia — as primeiras categorias com alguns itens, ou os itens em destaque. Manter o mesmo enquadramento visual e as chaves de tradução que a seção já usa (`home.services`).

- [ ] **Step 7: Limpar o sitemap**

Em `src/app/sitemap.ts`, remover as entradas geradas por `getServiceSitemapEntries` (a rota `/gastronomia/<slug>` não existe mais). Manter a entrada da própria `/gastronomia`.

- [ ] **Step 8: Validar**

```bash
npm run typecheck && npm run lint && npm run build && npm test
```

Esperado: exit 0. O build **não** deve mais listar `/[locale]/gastronomia/[slug]`.

- [ ] **Step 9: Conferir no navegador**

```bash
npm run start
```

Cadastrar pelo admin duas categorias com pratos, então abrir `/pt/gastronomia` e confirmar: as categorias aparecem na ordem, os pratos dentro delas, as etiquetas, o dia da semana nos pratos marcados, e **nenhum preço em lugar nenhum**. Conferir que o dropdown "Nossa Gastronomia" no header lista as categorias e que clicar leva à âncora certa. Encerrar o servidor.

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "CRE: /gastronomia passa a renderizar o cardapio

A pagina, a secao da home e o dropdown do header deixam de ler Service e
passam a ler o cardapio. Service continua no schema e no admin — sai no PR
seguinte, para nenhum PR intermediario deixar o site publico quebrado.

/gastronomia/[slug] sai agora: prato nao tem pagina propria, e o dropdown
passa a apontar para ancoras dentro da propria pagina do cardapio.

Nenhum preco renderizado, por diretriz do cliente.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Critérios de aceite do PR

- `npm run typecheck && npm run lint && npm run build` verde.
- `npm test` verde, com 4 arquivos (`contact-form`, `phone`, `rate-limit`, `menu-form`).
- `npx playwright test` verde.
- Banco com 12 models / 13 tabelas.
- É possível criar categoria e prato pelo admin em `/admin/cardapio`, e eles aparecem em `/gastronomia`.
- Apagar uma categoria apaga seus pratos, e a confirmação avisa disso antes.
- **Nenhum preço** no schema, no admin, na página ou no structured data.
- `Service`, `Client`, `Stat`, `TeamMember` e `Project` continuam intactos — a remoção deles é o PR seguinte.
- Nenhum `git push`.
